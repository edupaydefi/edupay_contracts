# RPC URL for the Ethereum node (e.g., Infura, Alchemy, or any public/private Ethereum node)
RPC_URL=https://eth-mainnet.alchemyapi.io/v2/YOUR_ALCHEMY_API_KEY

# Private key of the deploying account (use a private key with enough test/mainnet ETH for deployment)
PRIVATE_KEY=YOUR_PRIVATE_KEY

# Optional: Ethereum chain ID for network specification (mainnet: 1, Goerli testnet: 5, etc.)
CHAIN_ID=1

# Optional: Block explorer API key for contract verification (e.g., Etherscan API key)
ETHERSCAN_API_KEY=YOUR_ETHERSCAN_API_KEY

# Optional: Alchemy/Infura API Key (if using these services for RPC URL)
ALCHEMY_API_KEY=YOUR_ALCHEMY_API_KEY

# Optional: Gas price (set custom gas price, or rely on defaults)
GAS_PRICE=20000000000

# Optional: Gas limit (set custom gas limit, or rely on defaults)
GAS_LIMIT=3000000
